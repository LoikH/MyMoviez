<h1><%= @title %></h1>

<%= will_paginate %>

<ul class="userlist">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, :size => 30 %>
      <div><%= link_to user.username, user %></div>
    </li>
  <% end %>
</ul>

<%= will_paginate %>